namespace SDK
{
    class Program
    {
        static void Main(string[] args)
        {
            //Task.Factory.StartNew(() => GameMemReader.getInstance().RunLoop()); // run loop in background
            //AmongUsReader.getInstance().Run();
        }
    }
}
